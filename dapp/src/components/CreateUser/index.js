import './index.css';

function CreateUser() {
  return (
    <div className='createuser'>
      <p>Create User using create New block function (doesnt currently work)</p>
      <button type='button' className='create-user-button'>Create New User</button>
    </div>
  );
}

export default CreateUser;